# README

The files in this directory are practical tests and experiments, including
inputs and outputs, mostly using the Scopus API. These were all in the root
directory, so the python code will may not run outside of it. We moved these
temporary files here to avoid confusion in the public repo.
